// Criar uma função que aceita dois argumentos, somente do tipo númerico e exiba a soma entre os dois.

const a = 10;
const b = 5;

function sum(a: number, b: number) {
  return a + b;
}

console.log(sum(a, b));
