function sum(a: number, b: number) {
  return console.log(a + b);
}

sum(10, 10);

// não funciona pois o primeiro parâmetro não é um number
// sum("a", true)

// não funciona pois o segundo parâmetro não é um number
// sum(15, true)
